
\section{Introduction}
Large language models (LLMs) excel at reasoning, coding, and multimodal tasks~\cite{egi2025,staticanalysis2025,olfactory2025}. However, due to the high hardware requirements (RAM, GPU, etc.) they demand, these models are usually run only on cloud-based infrastructures or very advanced local systems~\cite{energytoken2025,qwqrouting2025}. This creates a significant bottleneck for system developers seeking to reduce cloud dependency, ensure data privacy, and avoid high operational costs~\cite{tigani2025cpu,aydin2025integrated}.

As a result of these limitations, small language models (SLMs) are emerging as a strong alternative. 
Their ability to run on local machines provides a significant advantage in terms of data security and deployment autonomy, while also offering substantially lower operational costs than cloud-scale LLM setups~\cite{tigani2025cpu,aydin2025integrated}. 
Moreover, recent studies show that SLMs can be adapted to specific domains via fine-tuning or distillation, enabling competitive performance in specialized tasks~\cite{xie2025fusing,distillingmath2024,rethinkllm2025}. 
In practical systems, SLMs are also attractive because they integrate well with explicit control modules (e.g., rule-based filters, routing/verification thresholds, or planners), improving controllability and reliability beyond prompt-only solutions~\cite{aydin2025integrated,qwqrouting2025,ji2025raprag}. 
However, SLMs still struggle to match the broad expertise and robust reasoning of massive frontier models, especially on tasks requiring deep contextual understanding and extensive world knowledge~\cite{egi2025,dorfner2024comparing}. 
Therefore, framing the problem as a simple ``SLM or LLM?'' competition is no longer sufficient; instead, current research investigates when a single SLM is adequate, when an LLM is necessary, and when hybrid or agent-based architectures are preferable~\cite{hao2024hybrid,shao2025dot,multiagent2025}.

The existing literature provides concrete, domain-specific examples showing that the choice between SLMs, LLMs, and hybrid designs is primarily a systems decision. In telecommunications, Symbiotic Agents reports up to a fivefold reduction in decision error and near-real-time operation (82~ms loops) with up to 99.9\% lower GPU overhead using SLM variants~\cite{chatzistefanidis2025symbiotic}. In medicine, self-reflective pipelines pair a compact generator with an LLM-based judge to iteratively refine outputs and improve diagnostic reliability~\cite{han2025chest}.

These developments provide practical alternatives for scenarios where relying on a single monolithic LLM is either insufficient for task demands or impractical under strict resource constraints. Accordingly, recent work increasingly targets measurable improvements beyond raw accuracy, including reduced computational load, lower operational cost, and tighter latency budgets~\cite{hao2024hybrid,qwqrouting2025,chatzistefanidis2025symbiotic,energytoken2025}. Across domains, researchers have reported which hybrid design patterns are effective for specific task classes---for example, selective edge--cloud verification, expert routing, and retrieval- or planner-centered pipelines---and how these choices translate to real-world deployments~\cite{hao2024hybrid,lee2024orchestrallm,alabbasi2024teleoracle,ji2025raprag}.

Motivated by these trends, this review analyzes when a single SLM is sufficient, when an LLM remains necessary, and when hybrid or multi-agent architectures provide the best trade-off between capability and efficiency~\cite{tigani2025cpu,xie2025fusing,hao2024hybrid,shao2025dot}. We further examine domain conditions under which fine-tuned SLMs can realistically replace or complement larger models, and where they still fall short, synthesizing recurring architectural motifs across telecom, medical, and cybersecurity settings~\cite{chatzistefanidis2025symbiotic,han2025chest,pietrzak2025evaluation}.

The main contributions of this review are as follows: (1) It provides a synthesized overview of 56 studies covering cloud-based monolithic LLMs, hybrid SLM--LLM architectures, the performance of fine-tuned SLMs versus large models, and multi-agent workflows. (2) It reframes the literature around four distinct practical research questions: Performance, Efficiency, Architecture, and Domain Suitability. (3) It creates an analytical review matrix that reveals the commonalities and differences among the studies, mapping which specific research question each study addresses.
\begin{figure}[htbp]
\centering
\resizebox{\columnwidth}{!}{%
\begin{forest}
for tree={
  grow'=east,
  parent anchor=east,
  child anchor=west,
  anchor=west,
  align=left,
  edge={draw=red!65, line width=0.7pt},
  calign=center,
  edge path={
    \noexpand\path[\forestoption{edge}]
    (!u.parent anchor) -- ++(12pt,0) |- (.child anchor)\forestoption{edge label};
  },
  l sep=16pt,
  s sep=14pt,
  font=\small,
  rounded corners,
  draw=red!60,
  line width=0.6pt,
  fill=red!3,
  inner xsep=7pt,
  inner ysep=4pt,
}
[{{SLR: Hybrid \& SLM Systems}}
  [{{Motivation (Intro)}}
    [{{LLM constraints: cost, latency, privacy~\cite{energytoken2025,qwqrouting2025,egi2025}}}]
    [{{SLM opportunities: local, adaptable, controllable~\cite{tigani2025cpu,aydin2025integrated,xie2025fusing}}}]
    [{{System view: hybrid / routing / agents~\cite{hao2024hybrid,lee2024orchestrallm,shao2025dot,multiagent2025}}}]
  ]
  [{{Corpus \& Method}}
    [{{PRISMA + Selection~\cite{page2021prisma}}}]
    [{{Data extraction (54 studies)}}]
    [{{Taxonomy / Labels (this review)}}]
  ]
  [{{Models / Architectural Families}}
    [{{Single SLM (local/on-device)~\cite{tigani2025cpu,aydin2025integrated}}}]
    [{{Hybrid SLM+LLM (edge--cloud)~\cite{hao2024hybrid}}}]
    [{{Multi-agent systems~\cite{multiagent2025,pacheco2025flight}}}]
    [{{RAG-centric pipelines~\cite{alabbasi2024teleoracle,ji2025raprag}}}]
    [{{Control / routing modules~\cite{lee2024orchestrallm,qwqrouting2025}}}]
  ]
  [{{Research Questions (RQs)}}
    [{{RQ1: Performance~\cite{hao2024hybrid,lee2024orchestrallm,han2025chest}}}
      [{{Hybrid SLM+LLM (draft--verify)~\cite{hao2024hybrid}}}]
      [{{Multi-agent collaboration~\cite{multiagent2025,pacheco2025flight}}}]
      [{{Self-reflective / judge-in-the-loop~\cite{han2025chest}}}]
    ]
    [{{RQ2: Efficiency~\cite{qwqrouting2025,energytoken2025,chatzistefanidis2025symbiotic}}}
      [{{Latency / VRAM / cost proxies~\cite{chatzistefanidis2025symbiotic,hao2024hybrid}}}]
      [{{Energy / energy-per-token~\cite{energytoken2025}}}]
      [{{Edge feasibility~\cite{tigani2025cpu,qwqrouting2025}}}]
    ]
    [{{RQ3: Architecture~\cite{alabbasi2024teleoracle,ji2025raprag,lee2024orchestrallm}}}
      [{{RAG + rerank / long-context support~\cite{alabbasi2024teleoracle}}}]
      [{{Planner / adaptive retrieval~\cite{ji2025raprag}}}]
      [{{Expert routing / orchestration~\cite{lee2024orchestrallm}}}]
    ]
    [{{RQ4: Domain suitability~\cite{xie2025fusing,aydin2025integrated,rethinkllm2025}}}
      [{{Fine-tuned SLMs / domain fusion~\cite{xie2025fusing}}}]
      [{{Privacy-sensitive local domains~\cite{aydin2025integrated}}}]
      [{{Where SLMs fall short (reasoning gap)~\cite{distillingmath2024}}}]
    ]
  ]
  [{{Contributions}}
    [{{(1) 54-study synthesis}}]
    [{{(2) RQ framing}}]
    [{{(3) Review matrix}}]
  ]
]
\end{forest}
}
\caption{Overview of the review structure and taxonomy used in this SLR (synthesized from the reviewed corpus; reporting guided by PRISMA~\cite{page2021prisma}).}
\label{fig:slr-overview}
\end{figure}
% \section{Findings}
% \subsection{Corpus-Level Trends Before the RQ Analysis}
% Before answering the four research questions directly, three corpus-level observations are worth emphasizing.

% First, the field is now primarily a systems literature rather than a pure model-scaling literature. Although a few studies still compare standalone small and large models directly, the dominant trend is to embed them inside a larger control strategy. Hybrid edge-cloud pipelines, retrieval-centered systems, expert routers, task decomposers, self-reflective workflows, and fusion modules are now common design choices. In other words, recent work increasingly treats model scale as one variable in a broader systems problem.

% Second, hybridization is motivated by different constraints in different domains. In telecom and networking, the main concerns are deployment, context fidelity, and latency budgets. In healthcare and scientific tasks, the dominant concerns are reliability, interpretability, and domain specificity. In industrial monitoring, multimodal hybridization is driven by the need to combine coarse global understanding with fine-grained local detail. In optimisation and education, hybrid methods often serve as a way to recover reasoning quality from small models through feedback, routing, or targeted prompting.

% Third, the evidence base is uneven. Accuracy is almost always reported; compute, latency, and cost are not. This matters because a large share of the literature frames hybridization as an efficiency solution, yet only a minority of studies expose sufficiently comparable numbers to test those claims rigorously. The result is a literature that often proves that hybridization \emph{can} work, but less often proves how much it costs in practice under standardized conditions.

% \subsection{RQ1: Comparative Performance of Hybrid and Multi-Agent Systems}
% Across the corpus, hybrid and multi-agent architectures generally improve the performance ceiling of SLM-centered systems and, in several cases, also outperform larger monolithic baselines. The main reason is not that small models suddenly become universally strong, but that coordination mechanisms route the right subtasks, tokens, or contexts to the right component at the right moment.

% The clearest evidence comes from edge-cloud collaboration. Hao et al.~\cite{hao2024hybrid} show that token-level draft-verification can recover LLM-like quality on GSM8K while using only 25.8\% of the full-cloud LLM cost. Their abstract and introduction clarify why this works: many tokens are easy enough for the on-device SLM, and only a small subset of difficult tokens must be verified or regenerated in the cloud. The key performance implication is that quality recovery does not require whole-query escalation. Token-level selectivity is enough.

% This pattern also appears in wireless and mobile settings. Oh et al.~\cite{oh2025uncertainty} frame hybrid inference as a distributed speculative-decoding problem and show that uncertainty-aware skipping can preserve up to 97.54\% of LLM accuracy. Their work is especially important for the present review because it ties performance directly to a measurable systems lever: if the SLM can estimate when its token is likely to be accepted, then the system can skip unnecessary communication and cloud inference without losing much quality.

% Routing can do more than merely preserve LLM quality. In dialogue state tracking, OrchestraLLM uses a retrieval-based router to select the most suitable expert among SLM and LLM pools~\cite{lee2024orchestrallm}. The website extraction reports 82.46\% turn-level joint goal accuracy on MultiWOZ and 68.09\% on SGD, outperforming both the LLM and SLM baselines. The corresponding PDF makes the reason more explicit: SLMs and LLMs exhibit complementary strengths, with SLMs handling schema and formatting constraints well and LLMs contributing commonsense and broader contextual reasoning. This is an example of a hybrid system exceeding both ends of the scale spectrum because the system exploits complementarity rather than averaging behavior.

% Domain-specific retrieval produces similarly strong results. TeleOracle adapts Phi-2 to telecom question answering by combining fine-tuning, semantic chunking, hybrid retrieval, reranking, and context-window extension~\cite{alabbasi2024teleoracle}. In the reviewed corpus, this system reaches 81.20\% accuracy, which is roughly 30\% above the base Phi-2 model and competitive with much
